
// Texture Buffer Object (TBO) Test OpenGL Fragment Shader

#version 130

#extension GL_ARB_texture_rectangle : enable

uniform sampler2DRect Tex;

in vec2 TexCoord;

out vec4 Color;

void main()
{
	Color = texture2DRect(Tex, TexCoord*vec2(1024.0));
}
